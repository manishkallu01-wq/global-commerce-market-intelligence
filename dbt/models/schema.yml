version: 2
models:
  - name: stg_product_offers
    columns:
      - name: source_url
        tests: [not_null]
      - name: crawl_id
        tests: [not_null]
      - name: currency
        tests: [not_null]
      - name: price
        tests: [not_null]
  - name: mart_price_position
    columns:
      - name: product_key
        tests: [not_null]
      - name: observed_offer_count
        tests: [not_null]
